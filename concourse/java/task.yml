platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: alpine

inputs:
  - name: source

outputs:
  - name: jar-file

run:
  path: sh
    args:
      - -exc
      - |
        source/concourse/java/task.sh
        mvn test
